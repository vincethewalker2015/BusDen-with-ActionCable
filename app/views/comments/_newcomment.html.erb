<div class="row">
  <div class="col-md-8 col-md-offset-2 ">
    <h3>Comments: </h3>
    <hr />
          <%= form_for([@user, @comment], remote: true, :html => {class: "form-horizontal", role: "form"}) do |f| %>
          <div class="form-group">
             
             <div class="col-md-8 col-md-offset-2">
               <%= f.text_area :description, rows: 5, class: 'form-control', placeholder: "Add comment" %>
             </div>
           </div>
          <div class="form-group">
            <div class=" col-md-8 col-md-offset-2">
              <%= f.submit "Submit comment", class: "'btn btn-primary copper" %> 
            </div>
          </div>
        <% end %>
        <hr />
      </p>
  </div>
</div>